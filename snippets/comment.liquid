<div class="comment">
  <div class="comment-meta">
    <strong><span>{{ comment.author }}</strong>
    <span><time datetime="{{ comment.created_at | date: '%Y-%m-%d' }}">{{ comment.created_at | date: format: 'month_day_year' }} {{ 'blogs.comments.comment_posted_at' | t }} {{ comment.created_at | date: '%H:%M%p' }}</time></span>
  </div>
  {{ comment.content }}
</div>
