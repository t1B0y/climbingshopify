{% if article.tags.size > 0 %}
  <span class="article-meta__item article-tags">
    {% for tag in article.tags %}
      <a href="{{ blog.url }}/tagged/{{ tag | handle }}">{{ tag | capitalize }}{% unless forloop.last %}, {% endunless %}</a>
    {% endfor %}
  </span>
{% endif %}
